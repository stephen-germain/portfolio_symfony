{% extends 'baseAdmin.html.twig' %}

{% block title %}Compétences{% endblock %}

{% block body %}

    <div class="container">
       <div class="row py-5">
            <div class="example-wrapper">
                <h3>Liste des compétences</h3>
            </div>
        </div>

      
            <div class="row">
                <div class="col-md-4">
                    <h4>langages</h4>
                    <table class="table table-bordered table-hover table-sm text-center">
                        <thead>
                            <tr>
                                <th>compétence</th>
                                <th>actions</th>
                            </tr>
                        </thead>
                        <tbody>
                          {% for skill in skills %}
                            <tr>
                                <td>{{skill.nom}}</td>

                                <td>
                                    <a href="{{path('skills_update', {id:skill.id}) }}">modifier</a>
                                    <a href="{{path('skills_delete', {id:skill.id}) }}">supprimer</a>
                                </td>
                            </tr>
                               {% endfor %}
                        </tbody>
                    </table>
                </div>
                <div class="col-md-4">
                    <h4>frameworks</h4>
                    <table class="table table-bordered table-hover table-sm text-center">
                        <thead>
                            <tr>
                                <th>compétence</th>
                                <th>actions</th>
                            </tr>
                        </thead>
                        <tbody>
                        {% for skill in skills %}
                            <tr>
                                <td>{{skill.framework}}</td>
                                <td>
                                    <a href="{{path('skills_update', {id:skill.id}) }}">modifier</a>
                                    <a href="{{path('skills_delete', {id:skill.id}) }}">supprimer</a>
                                </td>
                            </tr>
                               {% endfor %}
                        </tbody>
                    </table>
                </div>
                <div class="col-md-4">
                    <h4>cms</h4>
                    <table class="table table-bordered table-hover table-sm text-center">
                        <thead>
                            <tr>
                                <th>compétence</th>
                                <th>actions</th>
                            </tr>
                        </thead>
                        <tbody>
                        {% for skill in skills %}
                            <tr>
                                <td>{{skill.cms}}</td>
                                <td>
                                    <a href="{{path('skills_update', {id:skill.id}) }}">modifier</a>
                                    <a href="{{path('skills_delete', {id:skill.id}) }}">supprimer</a>
                                </td>
                            </tr>
                               {% endfor %}
                        </tbody>
                    </table>
                </div>
            </div> 
        <a href="{{ path('skills_create') }}" class="btn btn-outline-dark float-right">Ajouter</a>
    </div>

{% endblock %}
